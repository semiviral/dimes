[package]
name = "shard"
version = "0.1.0"
edition = "2021"


[dependencies]
lib = { path = "../lib" }

dotenvy = "*"

anyhow = "*"
tracing = "*"
tracing-subscriber = { version = "*", features = ["parking_lot"] }
config = "*"
redis = { version = "*", features = ["ahash", "tokio-comp"] }
deadpool = { version = "*", features = ["rt_tokio_1"] }
chacha20poly1305 = "*"

tokio = { version = "*", features = ["full"] }
serde = { version = "*", features = ["derive"] }
axum = { version = "*", features = ["tracing", "http2", "multipart"] }

once_cell = "*"
uuid = { version = "*", features = ["v7", "fast-rng"] }
